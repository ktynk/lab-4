#include <stdio.h>
#include <math.h>
#include <string.h>
int main()
{
    char symbols[10];
    int z;
    printf("vvedite elementi 10 ");
    for (z = 0; z < 10; z++)
    {
        scanf("%c", &symbols[z]);
        getchar();
    }
    for (int i = 0; i < 10; i++) {
        int flag = 1;
        for (int a = 0; a < 10; a++) {
            if (symbols[i] == symbols[a] && i!=a) {
                flag = 0;
                break;
            }
        } if (flag==1){
         printf("ne povtoraytsa:%c\n",symbols[i]);
        }
    }
    return 0;
}
