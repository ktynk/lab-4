#include <stdio.h>
#include <math.h>
int main(){
    char symbols[11];
    printf("vvedite elementi 10");
    scanf_s("%c %c %c %c %c %c %c %c %c %c",&symbols);
    int n =10;
    for(int i=0;i<n;i++){

    }
}for(int i = 0; i < n; i++){
    for(int a = 1+i: a<n; a++ ) if
    mass[i] == mass[a]
}
